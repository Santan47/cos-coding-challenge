import { CalcRemainingTimePipe } from './calc-remaining-time.pipe';

describe('CalcRemainingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcRemainingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
